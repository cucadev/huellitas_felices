doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Login - PetShop Huellitas Felices

    // Fuente Poppins
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
    link(href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet")
    
    // Bootstrap CSS
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet")

    style.
      body {
        font-family: 'Poppins', sans-serif;
        background-color: #5E0F3C;
      }
      .btn-custom {
        background-color: #9A4475;
        color: #fff;
      }
      .btn-custom:hover {
        background-color: #762051;
      }

  body
    .d-flex.justify-content-center.align-items-center.min-vh-100
      .card.shadow.p-4(style="max-width: 400px; width: 100%;")
        h2.text-center.mb-4(style="color:#762051") Iniciar Sesión
        form(action="/api/users/login" method="POST")
          .mb-3
            label.form-label(for="email") Email:
            input.form-control(type="email" name="email" required)
          .mb-3
            label.form-label(for="password") Contraseña:
            input.form-control(type="password" name="password" required)
          button.btn.btn-custom.w-100(type="submit") Ingresar
        p.text-center.mt-3
          | ¿No tenés cuenta? 
          a(href="/register" class="text-decoration-none fw-bold" style="color:#9A4475") Registrate

    // Bootstrap JS
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js")

